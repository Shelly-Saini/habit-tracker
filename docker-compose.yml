services:

  backend:
    build: ./backend
    container_name: habit-tracker-backend
    env_file:
      - ./backend/.env
    ports:
      - "5000:5000"

  frontend:
    build: ./frontend
    container_name: habit-tracker-frontend
    ports:
      - "3000:80"
    depends_on:
      - backend